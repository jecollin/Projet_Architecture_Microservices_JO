# Configuration du port d'écoute du service
server.port=8302

# Nom de l'application
spring.application.name=sport-service

# Configuration de la base de données MySQL
spring.datasource.url=jdbc:mysql://localhost:3306/sportdb?useSSL=false&serverTimezone=UTC
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.datasource.username=root
spring.datasource.password=root
spring.jpa.database-platform=org.hibernate.dialect.MySQL5InnoDBDialect

# Afficher les requêtes SQL dans la console pour le débogage
spring.jpa.show-sql=true

# Utiliser le mode 'update' pour la génération automatique du schéma
spring.jpa.hibernate.ddl-auto=update

# Configuration du niveau de logging (peut être "TRACE", "DEBUG", "INFO", "WARN", "ERROR")
logging.level.org.springframework=INFO

# Configuration du client Feign pour communiquer avec le microservice Olympic
feign.client.config.default.connectTimeout=5000
feign.client.config.default.readTimeout=5000

# URL du microservice Olympic
olympic-service.url=http://localhost:8083
